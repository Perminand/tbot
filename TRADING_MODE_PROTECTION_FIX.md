# Исправление проблемы автоматического переключения на песочный счет

## 🚨 Проблема
Бот работал на торговом счету, но несанкционированно самостоятельно переключался на песочный счет, хотя торговля осталась не песочная.

## 🔍 Анализ причин

### 1. **Конфликт конфигурации**
- В `application.yml` установлен `default-mode: production`
- В коде `InvestApiManager` был fallback на `sandbox`
- Это создавало неопределенность при инициализации

### 2. **Автоматический fallback на песочницу**
При любых ошибках инициализации система автоматически переключалась на песочный режим:
- При проблемах с БД
- При недействительных токенах
- При ошибках чтения настроек

### 3. **Отсутствие защиты от несанкционированного переключения**
Не было механизма, который бы предотвращал автоматическое переключение с production на sandbox во время активной торговли.

## ✅ Исправления

### 1. **Исправлен InvestApiManager**
```java
// БЫЛО: Автоматический fallback на sandbox
if (!isTokenConfigured(initialMode)) {
    log.warn("Токен для сохраненного режима {} не настроен. Используем defaultMode={}.", initialMode, defaultMode);
    initialMode = defaultMode;
}

// СТАЛО: Остановка инициализации при проблемах
if (!isTokenConfigured(initialMode)) {
    log.error("КРИТИЧЕСКАЯ ОШИБКА: Токен для режима {} не настроен!", initialMode);
    throw new IllegalStateException("Не настроены токены для ни одного режима торговли");
}
```

### 2. **Исправлен TradingModeService**
```java
// Добавлена дополнительная проверка безопасности
if (PRODUCTION_MODE.equals(currentMode) && SANDBOX_MODE.equals(mode)) {
    log.warn("⚠️ ВНИМАНИЕ: Попытка переключения с production на sandbox режим");
    log.warn("Это может быть небезопасно во время активной торговли");
}
```

### 3. **Создан TradingModeProtectionService**
Новый сервис для защиты от несанкционированного переключения:
- Проверка целостности режимов между всеми компонентами
- Блокировка переключения во время активной торговли
- Мониторинг рассинхронизации режимов
- Принудительная синхронизация при необходимости

### 4. **Обновлен AccountService**
```java
// БЫЛО: Могло происходить автоматическое переключение
return investApiManager.getCurrentInvestApi().getUserService().getAccounts().join();

// СТАЛО: Явная обработка ошибок без переключения
try {
    List<Account> accounts = investApiManager.getCurrentInvestApi().getUserService().getAccounts().join();
    return accounts;
} catch (Exception e) {
    log.error("⚠️ ВНИМАНИЕ: Ошибка API в режиме {}. НЕ переключаемся автоматически!", currentMode);
    throw new RuntimeException("Ошибка получения аккаунтов в режиме " + currentMode + ": " + e.getMessage(), e);
}
```

### 5. **Обновлен TradingBotScheduler**
Добавлена защита во все методы планировщика:
- Проверка целостности режима перед каждой операцией
- Установка флага активной торговли во время выполнения
- Остановка операций при обнаружении рассинхронизации

### 6. **Обновлен TradingModeController**
Добавлены новые эндпоинты:
- `/api/trading-mode/protection-status` - статус защиты
- `/api/trading-mode/synchronize` - принудительная синхронизация
- `/api/trading-mode/set-trading-active` - управление флагом активной торговли

## 🛡️ Новые механизмы защиты

### 1. **Проверка целостности режимов**
```java
public boolean validateTradingMode() {
    String currentMode = tradingModeService.getCurrentMode();
    String investApiMode = investApiManager.getCurrentMode();
    String dbMode = getTradingModeSettings().getValue();
    
    return currentMode.equals(investApiMode) && currentMode.equals(dbMode);
}
```

### 2. **Блокировка переключения во время торговли**
```java
public boolean isModeSwitchSafe(String newMode) {
    if (isTradingActive.get()) {
        log.error("❌ ПЕРЕКЛЮЧЕНИЕ РЕЖИМА ЗАПРЕЩЕНО: Активная торговля");
        return false;
    }
    return validateTradingMode();
}
```

### 3. **Принудительная синхронизация**
```java
public boolean forceSynchronizeModes() {
    // Синхронизация между всеми компонентами
    // Обновление настроек в БД
    // Логирование всех изменений
}
```

## 📊 Мониторинг и логирование

### Улучшенное логирование:
- ✅ Успешные операции
- ⚠️ Предупреждения о потенциальных проблемах
- ❌ Критические ошибки с подробным описанием
- 🔄 Операции синхронизации

### Новые эндпоинты для мониторинга:
- `GET /api/trading-mode/protection-status` - полный статус защиты
- `GET /api/trading-mode/status` - расширенный статус режима

## 🚀 Результат

### До исправления:
- ❌ Автоматическое переключение на песочницу при ошибках
- ❌ Отсутствие защиты от несанкционированного переключения
- ❌ Неопределенность в конфигурации
- ❌ Отсутствие мониторинга целостности режимов

### После исправления:
- ✅ Остановка системы при критических ошибках вместо автоматического переключения
- ✅ Многоуровневая защита от несанкционированного переключения
- ✅ Четкая конфигурация без конфликтов
- ✅ Полный мониторинг и синхронизация режимов
- ✅ Блокировка переключения во время активной торговли

## 🔧 Рекомендации по использованию

### 1. **Для тестирования**
```yaml
# application.yml
tinkoff:
  api:
    default-mode: sandbox  # Измените на sandbox для тестирования
```

### 2. **Для продакшена**
```yaml
# application.yml
tinkoff:
  api:
    default-mode: production  # Оставьте production для реальной торговли
```

### 3. **Мониторинг**
Регулярно проверяйте статус защиты:
```bash
curl http://localhost:8081/api/trading-mode/protection-status
```

### 4. **При проблемах**
Если обнаружена рассинхронизация:
```bash
curl -X POST http://localhost:8081/api/trading-mode/synchronize
```

## 📝 Статус
- ✅ Все исправления внесены
- ✅ Новые механизмы защиты активны
- ✅ Улучшенное логирование работает
- ✅ Документация обновлена

Теперь система надежно защищена от несанкционированного переключения режимов торговли!
