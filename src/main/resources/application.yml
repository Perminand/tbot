spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/tbot_db
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

# Tinkoff Invest API Configuration
tinkoff:
  api:
    # Токен для песочницы (тестовый)
    sandbox-token: ${TINKOFF_SANDBOX_TOKEN:t.CRkcaE_K19MSklxNKVqJ2rcohwl5CGpwN6Hxp58mdase_kEotm5eXbt2PAlURvHZ6UkhGEPueod2VM7Op4jChg}
    # Токен для реальной торговли (продакшн)
    production-token: ${TINKOFF_PRODUCTION_TOKEN:t.RNl-ZpAkTj4Yl3DVSNBsQbW8xKifjbGSUrtQ-o05nWgwiod5K5cyNZaj_Q6D2N4_yl6UFfEH-_bwd9sjozO5ag}
    # Режим по умолчанию
    default-mode: sandbox  # sandbox или production

# Application Configuration
app:
  name: Tinkoff Trading Bot
  version: 1.0.0

# Logging Configuration
logging:
  level:
    ru.perminov: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE 
server:
  port: 8081

tbank:
  api:
    base-url: https://api.tbank.ru/invest
  